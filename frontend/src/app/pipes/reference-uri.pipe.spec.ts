import { ReferenceUriPipe } from './reference-uri.pipe';

describe('ReferenceUriPipe', () => {
  it('create an instance', () => {
    const pipe = new ReferenceUriPipe();
    expect(pipe).toBeTruthy();
  });
});
