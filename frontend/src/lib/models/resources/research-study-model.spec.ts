import { ResearchStudyModel } from './research-study-model';

describe('ResearchStudyModel', () => {
  it('should create an instance', () => {
    expect(new ResearchStudyModel({})).toBeTruthy();
  });
});
