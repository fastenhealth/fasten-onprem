# Production Environment Configuration

# Use pre-built image from GitHub Packages
FASTEN_IMAGE=ghcr.io/fastenhealth/fasten-onprem:main

# Production environment
FASTEN_ENV=prod

# Network and port configuration
# Limit access to localhost only (change for public access)
FASTEN_HOST=127.0.0.1
FASTEN_PORT=9090

# Paths for data persistence
FASTEN_DB_PATH=./prod_db
FASTEN_CACHE_PATH=./prod_cache
# FASTEN_CONFIG_PATH=./config.prod.yaml

# Container configuration
FASTEN_RESTART=unless-stopped

# Resource limits for production environment
FASTEN_CPU_LIMIT=1.0
FASTEN_MEMORY_LIMIT=2G