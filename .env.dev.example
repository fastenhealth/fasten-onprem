# Development Environment Configuration

# No image specified for development - will build from source
FASTEN_IMAGE=

# Build configuration
FASTEN_BUILD_CONTEXT=.
FASTEN_DOCKERFILE=Dockerfile
FASTEN_ENV=sandbox  # Use 'sandbox' mode for development

# Network and port configuration
FASTEN_HOST=127.0.0.1  # Limit access to localhost only
FASTEN_PORT=9090

# Paths for data persistence
FASTEN_DB_PATH=./dev_db
FASTEN_CACHE_PATH=./dev_cache
# FASTEN_CONFIG_PATH=./config.dev.yaml

# Container configuration
FASTEN_RESTART=no  # Don't restart automatically in development

# Resource limits for development environment (more relaxed)
FASTEN_CPU_LIMIT=0.5
FASTEN_MEMORY_LIMIT=1G